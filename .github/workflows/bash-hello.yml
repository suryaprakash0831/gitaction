name: Hello via Bash Script

on:
  push:
    branches: [ "main" ]

jobs:
  hello-bash:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hello Script
        run: ./say_hello.sh
